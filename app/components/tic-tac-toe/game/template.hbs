<TicTacToe::Game::Interface
  @onStart={{this.onStart}}
  @onReset={{this.onReset}}
  @winner={{this.winner}}
/>

<TicTacToe::Game::GridDisplay
  @blocks={{this.blocks}}
  @isStarted={{this.isStarted}}
  @onClick={{this.onClick}}
/>