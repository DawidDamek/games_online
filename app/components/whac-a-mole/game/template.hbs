<WhacAMole::Game::Interface
  @timer={{this.timeInSeconds}}
  @shouldBeDisabled={{this.shouldBeAbleToChangeDifficulty}}
  @isStarted={{this.isStarted}}
  @onStart={{this.moveMole}}
  @onStop={{this.stopMole}}
  @onEasy={{this.onEasy}}
  @onMedium={{this.onMedium}}
  @onHard={{this.onHard}}
  @onReset={{this.onReset}}
/>

<WhacAMole::Game::Hits @score={{this.score}} @finalScore={{this.finalScore}} />

<WhacAMole::Game::GridDisplay
  @squares={{this.squares}}
  @onCatch={{this.addPoint}}
/>

{{#if this.isShowSharedModal}}
  <Shared::Modal
    @title='Your final score'
    @bodyContent='Congratulations! Your score is:'
    @bodyAttribute={{this.finalScore}}
    @onClose={{this.onHideModal}}
  />
{{/if}}